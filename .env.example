# Copy to `.env` to enable analytics in a production build.
# When set (and only in `astro build`, not dev), SEO.astro emits one deferred, cookieless
# Plausible script for this domain. Leave unset for no analytics at all (research.md R6).
PUBLIC_ANALYTICS_DOMAIN=leesinclair.dev
